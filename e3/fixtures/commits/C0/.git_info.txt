# C0 — declared correctly (PPT slide 24)
tag        = C0
sha        = 84c3fcf26a6d0746e645aae2f1b7bde8f050130d
based_on   = synthetic commit (INSTRUCTOR_SYNTHESIZED)
scenario   = E3 EChecker C0/C1/C2 baseline
provenance = INSTRUCTOR_SYNTHESIZED

# This is a synthetic commit created for the E3 EChecker baseline:
# main.c reads only config.h; Makefile correctly declares main.o: main.c config.h.
# `scripts/run_lab.py` initialises the repo, commits and tags it, then writes
# the resulting hash into this file. Commit dates are fixed (see COMMIT_DATES
# in run_lab.py), so the hash above is reproducible on any machine rather than
# being a one-off value from a single run.